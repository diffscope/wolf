{
  "$id": "https://raw.githubusercontent.com/diffscope/wolf/main/docs/schemas/linguist-1-exports.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "org.openvpi.wolf.linguist.WolfLinguist Level 1 exports",
  "type": "object",
  "properties": {
    "phonemes": {
      "$ref": "https://raw.githubusercontent.com/diffscope/wolf/main/docs/schemas/string-set.schema.json",
      "description": "The content phonemes the language's own closed chain may end with. Reserved phonemes such as SP, AP and EP are not listed. Complete unless openSet is true."
    },
    "openSet": {
      "type": "boolean",
      "default": false,
      "description": "Whether output may fall outside the declared list. False, the default, means the list is complete and reads exactly as it always has. True means the list is the part this module knows about, which is what a chain ending in \"output the word itself when nothing matched\" has to say: its output is unbounded, so a host knows a static comparison against the list is incomplete rather than authoritative."
    }
  },
  "required": [
    "phonemes"
  ],
  "additionalProperties": false
}
