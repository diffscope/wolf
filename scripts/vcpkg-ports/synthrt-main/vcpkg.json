{
  "$schema": "https://raw.githubusercontent.com/microsoft/vcpkg-tool/main/docs/vcpkg.schema.json",
  "name": "synthrt-main",
  "version-string": "onnxruntime-builds-uptake",
  "description": [
    "SVS Synthesis Runtime Library, main line.",
    "Deliberately not named synthrt: the shared overlay carries a port of that name pinned to the refactor line, and a same-named port here would silently shadow it because this overlay is searched first."
  ],
  "homepage": "https://github.com/diffscope/synthrt",
  "license": "Apache-2.0",
  "dependencies": [
    {
      "name": "vcpkg-cmake",
      "host": true
    },
    {
      "name": "vcpkg-cmake-config",
      "host": true
    },
    "qmsetup",
    "stduuid",
    "stdcorelib",
    "stdcorelib-plugin",
    "blake3",
    "sparsepp",
    "bit7z"
  ],
  "features": {
    "onnx": {
      "description": "Build dsinfer and its ONNX Runtime driver (DirectML on Windows, CPU elsewhere)",
      "dependencies": [
        "onnxruntime-builds"
      ]
    }
  }
}
